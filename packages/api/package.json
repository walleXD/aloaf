{
  "name": "@loaf/api",
  "version": "0.1.0",
  "main": "lib/index.js",
  "license": "MIT",
  "private": true,
  "devDependencies": {
    "@types/micro": "^7.3.3",
    "@types/node": "^12.6.8",
    "concurrently": "^4.1.1",
    "micro-dev": "^3.0.0",
    "rimraf": "^2.6.3",
    "typescript": "^3.5.3"
  },
  "scripts": {
    "build": "rimraf lib && tsc",
    "dev": "yarn build && concurrently \"tsc --watch\" \"micro-dev\""
  },
  "dependencies": {
    "apollo-server-micro": "^2.7.0",
    "graphql": "^14.4.2"
  }
}
